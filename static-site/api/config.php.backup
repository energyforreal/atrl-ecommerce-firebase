<?php
// Private config for PHP (do NOT commit real secrets to public repos)
// Prefer setting env vars in Hostinger dashboard. This file is optional.

return [
  'RAZORPAY_KEY_ID' => 'rzp_live_RKD5kwFAOZ05UD', // Your Razorpay Key ID
  'RAZORPAY_KEY_SECRET' => 'msl2Tx9q0DhOz11jTBkVSEQz', // Your Razorpay Secret Key
  // IMPORTANT: This must be the secret string configured in Razorpay webhook settings, not the URL
  // Prefer setting via env var on the server; falling back to the provided value if not set.
  'RAZORPAY_WEBHOOK_SECRET' => getenv('RAZORPAY_WEBHOOK_SECRET') ?: 'Rakeshmurali@10',

  // SMTP configuration for Brevo SMTP (PHPMailer Primary)
  // For production, prefer setting these via environment variables
  'SMTP_HOST' => getenv('SMTP_HOST') ?: 'smtp-relay.brevo.com',
  'SMTP_PORT' => getenv('SMTP_PORT') ?: 587,
  'SMTP_USERNAME' => getenv('SMTP_USERNAME') ?: '8c9aee002@smtp-brevo.com',
  'SMTP_PASSWORD' => getenv('SMTP_PASSWORD') ?: 'FXr1TZ9mQ0aEVqjp',
  'SMTP_SECURE' => getenv('SMTP_SECURE') ?: 'tls', // tls for 587, ssl for 465
  'MAIL_FROM' => getenv('MAIL_FROM') ?: 'info@attral.in',
  'MAIL_FROM_NAME' => getenv('MAIL_FROM_NAME') ?: 'Lokesh Murali - ATTRAL Electronics',
  
  // Email system configuration
  'EMAIL_PRIMARY' => 'phpmailer', // 'phpmailer' or 'brevo'
  'EMAIL_FALLBACK' => 'brevo', // fallback when primary fails
];
?>
